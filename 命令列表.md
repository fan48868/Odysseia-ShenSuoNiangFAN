
docker compose logs bot_app -f
显示日志
docker compose down
关机
docker compose up -d
开机
docker compose up -d --build
重建


github命令

git commit -m "你的提交说明"
提交
git push origin shen_suo
推送
git pull --ff-only origin shen_suo
拉取